[project]
name = "sourcegraph-code-nav-intent-gate"
version = "1.0.0"
description = "Deterministic intent-bounded code graph navigation with auditable receipts"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
